#include <bits/stdc++.h>
using namespace std;

// Functions provided to the solution.
int GetCount(const vector<int> &S);
long long GetRank(const vector<int> &S);

string Recover(int n, int m) {
  // 這邊填入程式碼，下面這個是與題本相符的範例。
  vector<int> S1 = {0, 1, 2};
  vector<int> S2 = {1, 2, 3, 4, 5};
  int two = GetCount(S1);
  int three = GetRank(S1);
  int one = GetCount(S2);
  return "BAAAAA";
}
